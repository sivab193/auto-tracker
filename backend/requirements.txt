# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
email-validator==2.2.0

# --- Config / settings ---
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Database ---
SQLAlchemy==2.0.36
# psycopg[binary]==3.2.3   # uncomment to use Postgres instead of SQLite

# --- Auth ---
# Primary hashing scheme is pbkdf2_sha256 (stdlib); bcrypt is optional and
# pinned <4.1 for passlib 1.7.4 compatibility if you switch schemes.
passlib==1.7.4
bcrypt<4.1
PyJWT==2.10.1

# --- Object storage ---
minio==7.2.13

# --- OCR (optional at runtime; system needs the `tesseract` binary too) ---
pytesseract==0.3.13
pdf2image==1.17.0
Pillow==11.1.0

# --- Scheduling ---
APScheduler==3.11.0

# --- Telegram bot (optional at runtime) ---
python-telegram-bot==21.9

# --- HTTP client ---
httpx==0.28.1
